import React from 'react';

const LogOutButton = () => {

  return (
    <button
      className="border-none text-gray-700 hover:text-gray-900"
      type="button"
      onClick={() => {}}
    >
      Sign Out
    </button>
  );
};

export { LogOutButton };
